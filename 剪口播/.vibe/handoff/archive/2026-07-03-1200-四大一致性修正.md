# HANDOFF — 剪口播（2026-07-03）：四大一致性/同步修正落地

> 前一會話的最後狀態，給下一個會話接續用。
> 接手先讀這份 + memory（export-drift-timeline-map、silence-removal-stt-limits）。
> 歷史 handoff 在 `.vibe/handoff/archive/`。

## ✅ 這次完成了什麼（commit 9e2d9dc，已推 origin/main）

使用者四個要求全部實作，全部有測試：

1. **無損模式口型同步**（`scripts/cut_video.sh`）：無損 >1 段一律走單趟 trim/atrim（原被排除在單趟外）。影片維持 CRF17，音訊改一次性 256k AAC（copy 過不了濾鏡；使用者拍板口型同步優先）。實測挑書 31 段：舊多段 +0.35s、新單趟 ±0.00s、9.35x 實時。
2. **匯出後逐字對帳**（`scripts/verify_export.js` 檢查 4，FAIL 級）：SRT 實檔文字 vs 保留字逐字比對，一字之差即 FAIL。新增 `scripts/kept_words.js` 當「哪些字算保留」單一事實來源（發音區＝字跨度扣實測靜音，被刪 >50% 才丟），SRT/TXT/verify/refine 四方共用。training_server 兩條匯出路徑都自動帶 `--srt/--subtitles/--silences`。
3. **刀口邊界字原子化**（`scripts/refine_segments.js` Step C）：切點落在字發音區中間 → >50% 補刀刪完整字、≤50% 退刀保整字；碎屑 ≤0.09s（`word_atomic.sliver_sec`）容忍，不與切點吸附的 RMS 波谷刀點打架。
4. **停頓壓平文意分流**（同檔）：句末標點（。！？）後停頓＝轉場留 long_target(0.8s)、句中停頓＝氣口壓 target(0.45s)；轉錄句末標點 <3 個自動退回長度分流。`pause_flatten.semantic:false` 可關。
5. **near-exact 重錄**（`scripts/detect_retakes.js` fuzzy 層）：編輯距離 ≤1 時 take 下限 5→4 字；有校正稿且兩 take 都在稿裡不標（防列舉誤判）；evidence 標 `near-exact`、殘段門檻獨立 0.35s。
6. **音畫漂移 WARN→FAIL**（verify_export.js）。

測試：refine 25 / retakes 19 / merge 12 全過；真實素材全鏈路（619 字字幕→無損匯出→SRT→逐字對帳 100%）通過。`testing.md` 已同步。

## 🚧 還沒做完 / 待驗證

- **真片實剪驗收還沒跑**：以上都是單元測試＋合成刪除清單的實測。下一支影片從 8900 全流程（處理→審核→匯出）跑一次，看 verify 五項檢查（尤其逐字對帳、音畫漂移 FAIL 級）在真實素材上的表現。
- **文意分流只在帶標點的轉錄生效**：挑書邏輯的舊 whisper 轉錄無標點→自動退回長度分流（分流=長度）。BytePlus 新片要確認轉錄帶標點、semantic 真的啟動（refine stderr 會印「分流=文意」）。
- **無損音訊改 256k AAC**：使用者已知情同意，但還沒實際聽過成品。若使用者聽感有意見，可調 `cut_video.sh` 單趟無損分支的 `-b:a 256k`。

## 📌 關鍵決策（下次別再爭論）

- 口型同步 > 音訊零重編（無損模式音訊重編一次，2026-07-03 使用者拍板）。
- 字級去留判斷只能走 `kept_words.js`，任何新消費者不准自己複製 50% 規則。
- 原子化的碎屑容忍 0.09s 必須 > cut_snap.max_intrude_sec(0.06)，否則跟吸附打架（測試 6 實證）。
- near-exact 有校正稿時要求合併證據，防「台北市/台北縣」列舉誤砍。

## 📂 下個會話最重要的檔案

- `scripts/kept_words.js` ← 新增，單一事實來源
- `scripts/refine_segments.js` ← Step C 原子化＋文意分流
- `scripts/cut_video.sh` ← 無損單趟（搜「無損單趟」）
- `scripts/verify_export.js` ← 檢查 4 逐字對帳＋AV FAIL
- `scripts/training_server.js` ← runVerify extra 參數、兩處匯出接線
- `testing.md` ← L3/L4 清單已更新

## ⏭️ 下一步從哪繼續

1. 讀這份 HANDOFF。
2. 問使用者拿一支新口播，從 8900 `/cut` 全流程跑（或使用者自己跑完回報）。
3. 盯匯出 log 的 verify 區塊：五項應全 ✅；「逐字對帳」有 FAIL 就是接線問題，先查 SRT 是否用到舊刪除檔。
4. 順帶確認 refine stderr 印「分流=文意」（BytePlus 轉錄帶標點才會）。
5. 若 8900 在跑舊碼記得重啟（port 8900 PID → Stop-Process → `Start-Process node training_server.js`，cwd=scripts）。

## 🗑️ 這次會話的雜訊（下次別重新討論）

- verify_export.js 曾被 PowerShell 5.1 編碼（Get-Content 無 -Encoding + -replace 回寫）弄成亂碼，已 `git checkout --` 還原重做。教訓：**改 UTF-8 中文檔一律用 Edit/Write 工具，不用 PowerShell 字串回寫**。
- cut_video.sh 在 Git Bash 手動測試要傳 `E:/...` Windows 風格路徑，傳 `/e/...` MSYS 風格 ffprobe 會靜默失敗（DURATION 空→node -e SyntaxError）。
- 舊 HANDOFF（2026-06-30 BytePlus/三格化）的「端到端沒跑通」斷點後來已在 7/01-02 修掉（見 memory autoselected-bridge-bug）。
